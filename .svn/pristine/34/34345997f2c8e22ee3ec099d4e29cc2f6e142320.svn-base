<?php
		
	//Nome que vai ser usado na primeira posição do array $_SESSION para distinguir as sessions
	define('SESSION_NAME', "SESSION_" . str_replace('/', '', WEB_BASEPATH));

	/**
	 * Por default o sistema salva um histórico de navegação em session para que seja possível retroceder.
	 * Defina aqui qual o tamanho desse histórico
	 */
	define('NAV_HISTORY_SIZE', 5);
	
	
	define('DEFAULT_CONTROLLER_NAME', 'index');
	define('DEFAULT_ACTION_NAME', 'index');
